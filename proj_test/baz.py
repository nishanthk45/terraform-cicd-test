import numpy as np

def baz(a: np.ndarray, b: np.ndarray) -> np.ndarray:
    """
    Multiply two numpy arrays together.

    Args:
        a (np.ndarray): First array
        b (np.ndarray): Second array
    
    Returns:
        np.ndarray: Product of the two arrays
    """

    return a * b